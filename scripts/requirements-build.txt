# Lightweight deps for local/cached rebuilds of scripts/build_team_locations.py.
# GitHub Actions weekly workflow installs requirements.txt instead (full TSDB/MLB imports).
requests>=2.31.0
timezonefinder>=6.2.0
thesportsdb>=0.3.1

# Premium TheSportsDB key: set THESPORTSDB_API_KEY (CI) or ppv_thesportsdb_api_key in app Settings.
# Premium keys require V2 API (X-API-KEY header) for league team lists; free key "3" uses V1 SDK.
# WNBA league lists require a premium V2 key.
